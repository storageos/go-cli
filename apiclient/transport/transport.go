// Package transport contains types responsible for the encoding of storageos CLI
// resources to a format accepted by the API, transport of encoded data decoding returned data and
// abstracting the communication between the CLI and the API.
package transport
